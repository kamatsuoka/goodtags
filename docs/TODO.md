- scripts/integration-test-db-swap.sh + e2e/maestro/db-swap-*.yaml are dead. I verified this: the script blocks on SWAP_LOG_MARKER="Done updating DB from remote", which this branch renamed, so it now always times out — and its actual premise ("swaps it in mid-session… queries must run against the swapped-in handle") is the exact behavior the branch removed. Just repointing the marker would make it pass vacuously, which is worse than failing. This needs your call: retarget it at next-launch adoption, or delete it. I didn't want to delete 180 lines of your test infrastructure unasked.
- Efficiency: search runs the tag query 4× per search (the subSelect re-evaluates the full FTS + LIKE '%q%' predicate inside both the tracks and videos queries, plus a fourth for the count). Real and worth fixing, but it's pre-existing logic the diff only reindented, and rewriting search SQL is a correctness change, not a cleanup.
- Efficiency: force-refresh always re-downloads; failed init re-seeds per call; refresh leaks the old handle. All three are the deliberate, documented intent of the commits (Make the Data-page refresh a force refresh, Recover from DB initialization failures…). Adding cooldowns would change behavior.
- __resetDbStateForTest → jest.resetModules() and the ~100 lines of duplicated mock factories in the 1050-line test file. Both are genuine, but they're a test-harness refactor with real regression risk against a suite that currently encodes several hard-won bug fixes.
- The _backup.yaml e2e change grafts a ~3.6 MB download onto the favorites screenshot flow. Where that belongs is an e2e-organization judgment, not a code cleanup.

